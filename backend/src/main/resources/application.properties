spring.datasource.url=jdbc:postgresql://databanken.ucll.be:51819/2TX35?ssl=true&sslfactory=org.postgresql.ssl.NonValidatingFactory&sslmode=prefer
spring.jpa.properties.hibernate.default_schema=swapbook
spring.datasource.username=r0663200
spring.datasource.password=Wizzy12051998
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
#
# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto=create-drop
# Hibernate disable invocationtargetexception (error in log anders en fail to build soms)
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults = false